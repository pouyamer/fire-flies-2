import { JitterConfig } from "../types";
import { Utilities } from "../utilities";

export const jitterConfig: JitterConfig = {
    jitterX: {
        value: 0,
    },
    jitterY: {
        value: 0,
    },
    jitterPolarAngle: {
        value: 0,
    },
    jitterPolarAmount: {
        value: 0,
    }
}
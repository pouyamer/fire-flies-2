import { Message } from "../types";

export const MESSAGE: Message = {
  CanvasElementDoesNotExist: "Canvas element does not exist", // error
  CanvasRenderingContextDoesNotExist: "Canvas rendering context does not exist", // error
};